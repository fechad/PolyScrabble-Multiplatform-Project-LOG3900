import 'package:flutter/material.dart';

const accent = Color(0x007daf6b);
const accentWhite = Color(0x00F9FFF6);
const accentHover = Color(0x00608b50);
const accentDisabled = Color.fromRGBO(125, 175, 107, 0.5);
const accentDisabledFont = Colors.white;
const chatButtonHover = Color.fromRGBO(0, 0, 0, 0.05);
const basicFontColor = Colors.black;
const theirChatBubbleBackground = Color(0x00e6e6e6);
const navBarBackground = Color(0x004b6940);
const navBarIconBackground = Colors.white;
const sectionBackground = Colors.white;
const basicBoardTileFontColor = Color(0x00666666);
const basicBoardTileColor = Color(0x00ffebce);
const letter2TileColor = Color(0x00bbbeca);
const letter3TileColor = Color(0x00a8bbff);
const word2TileColor = Color(0x00ffc7c7);
const word3TileColor = Color(0x00e8a1a1);

ThemeData lightTheme = ThemeData(
    brightness: Brightness.light, primaryColor: const Color(0x007daf6b));

ThemeData darkTheme = ThemeData(
    brightness: Brightness.dark, primaryColor: const Color(0x007865DC));
